import { Injectable } from '@nestjs/common';
// import { InjectModel } from '@nestjs/mongoose';
// import { Model } from 'mongoose';

@Injectable()
export class MongoConnectorService {
  // constructor(
  //   @InjectModel(Task.name) public readonly taskModel: Model<Task>,
  // ) {}
}
